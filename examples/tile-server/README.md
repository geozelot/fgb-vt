# Tile Server Example

Interactive map demo serving US Counties as vector tiles via all three API tiers (`TileServer`, `TileClient`, `tile()`) and both `LocalConnector` and `HttpConnector`.

## Run

```bash
npm install
npm start
# → http://localhost:3000
```
